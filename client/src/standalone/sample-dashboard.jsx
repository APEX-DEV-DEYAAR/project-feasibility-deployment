import React from "react";
import ReactDOM from "react-dom/client";
import SampleDashboard from "../../../server/src/features/feasibility/sample.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SampleDashboard />
  </React.StrictMode>
);
